各種オレオレgyazoで出力されたファイルの一覧表示と管理を行ないます。
一般的なphp環境で動きますが、HTML_emplate_ITだけpearのライブラリなので導入しないといけません。
http://pear.php.net/package/HTML_Template_IT

gyazoで画像が出力されているディレクトリの下に適当なディレクトリを作成して、リポジトリ内のファイルを一式置いてください。
.htaccessに.htpasswordを指定する部分があるので、そちらで適宜パスワードを指定してください。
別に全体公開でいいなら.htaccess自体を消してくれてもいいです。

あとはブラウザからアクセスするだけです。
簡単ですね。
